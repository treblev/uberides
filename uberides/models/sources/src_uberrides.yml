version: 2

sources:
  - name: raw_ext
    database: UBERIDES
    schema: RAW_EXT
    tables:
      - name: rides_ext_with_loadts
        loaded_at_field: loaded_at
        freshness:
          warn_after:
            count: 36
            period: hour
          error_after:
            count: 48
            period: hour
  - name: bronze
    database: UBERIDES
    schema: BRONZE
    tables:
     - name: rides_base 